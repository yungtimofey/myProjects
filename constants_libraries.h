#include <iostream>
#include <vector>
#include <exception>
#include <algorithm>
#include <regex>

#define HEAP_SIZE 128
#define byte uint8_t
#define uint unsigned int
#define HEADER sizeof(uint)

#define sign bool
#define SIGN_SIZE sizeof(sign)

#define link byte*
#define LINK_SIZE sizeof(link)